{
  "name": "waeweb",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "WAE WEB — secure opt-in web reader with isolated persistent research vaults",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test",
    "check": "node --check server/index.mjs && node --check server/search.mjs && node --check server/intelligence.mjs && node --check server/reader.mjs && node --check public/app.js && node --check public/workspace.js && node --check server/vault.mjs"
  },
  "license": "UNLICENSED"
}
