{
  "name": "waeweb",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "WAE WEB — federated search and opt-in public HTTPS research index",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test",
    "check": "node --check server/index.mjs && node --check server/search.mjs && node --check server/intelligence.mjs && node --check server/reader.mjs && node --check public/app.js && node --check public/workspace.js"
  },
  "license": "UNLICENSED"
}
